## Prerequisites ()
>=Python 3.8.10
source venv_pip.sh

### Usage
1. Скопировать все файлы захвата трафика в папку "data_in"

### help
python -m pip  install -r requirements.txt --no-index -f e:\\temp\\python_Library

### Usage (Windows)

